# Self-Reliance FoCo v2.11.0

Large-print layout: headers, search, chips, and tabs wrap instead of clipping when Android font size is turned up. The Have a disability? tab uses a shorter header and a **Disability** tab label so more of the list stays on screen.

Same 339 listings. versionCode 17 — installs over v2.10.0.

The QR code and download button still use `/releases/latest/download/Self-Reliance-FoCo.apk`.
